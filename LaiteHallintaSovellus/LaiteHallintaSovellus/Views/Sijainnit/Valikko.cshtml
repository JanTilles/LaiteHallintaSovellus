@model IEnumerable<LaiteHallintaSovellus.Models.Sijainnit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pvm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Laitteet.Tyyppi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Varastot.Varasto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pvm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Laitteet.Tyyppi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Varastot.Varasto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Sijainti_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Sijainti_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Sijainti_ID })
        </td>
    </tr>
}

</table>
